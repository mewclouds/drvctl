namespace DrvCtl.Offline;

internal static class OfflineControlSetSelector
{
    internal const string PrototypeControlSet = "ControlSet001";
    internal const string Limitation = "Prototype limitation: ControlSet001 is selected explicitly; no universal active-control-set rule is claimed.";
}
